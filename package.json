{
  "name": "raoulyy-blog",
  "private": true,
  "version": "1.0.0",
  "description": "Static blog generated with Eleventy",
  "scripts": {
    "build": "eleventy",
    "deploy": "npm run build && node tools/generate-sitemap.js && node tools/copy-docs.js",
    "serve": "eleventy --serve",
    "watch": "eleventy --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
